import 'package:flutter/material.dart';

class ShellSettingScreen extends StatelessWidget {
  const ShellSettingScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Icon(Icons.settings, size: 120),
    );
  }
}
