import 'package:flutter/material.dart';

class ShellPlayScreen extends StatelessWidget {
  const ShellPlayScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Icon(Icons.play_arrow, size: 120),
    );
  }
}
